function calcularOperacoes(operador1, operador2) {
  console.log(
    "soma:",
    operador1 + operador2,
    " subtracao:",
    operador1 - operador2,
    " multiplicacao:",
    operador1 * operador2,
    " divisao:",
    operador1 / operador2
  );
}

calcularOperacoes(10, 20);
