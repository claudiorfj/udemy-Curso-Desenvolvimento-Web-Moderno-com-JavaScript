import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ai Que Diliça',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
